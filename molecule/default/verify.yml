---
- name: Verify
  hosts: all
  become: yes
  gather_facts: yes

  tasks:
    - name: check if connection still works
      ping:

    - name: check if port 3389 is open
      wait_for:
        port: 3389
        delay: 10
